import 'filter_state.dart';
import 'search_state.dart';

extension SearchStateExt on SearchState {
  /// Builds new [SearchState] with filters.
  SearchState withFilters(Filters filters) {
    throw UnimplementedError();
  }
}
